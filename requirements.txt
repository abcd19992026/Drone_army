# GSS runtime dependencies. Pinned deliberately -- the dock Pi runs this
# 24/7 and must not pick up surprise upgrades.
pymavlink==2.4.42       # MAVLink
python-dotenv==1.0.1    # .env loading
websockets==13.1        # Supabase Realtime (sync client); v0.3 command intake fast path
tzdata==2026.4          # IANA tz database for zoneinfo (v0.9 gss/scheduler.py).
                        # stdlib zoneinfo has no bundled database on Windows,
                        # and cannot be assumed present on every Linux image
                        # either -- this makes Asia/Kolkata conversion work
                        # the same regardless of the host OS's own tz data/setting.
# Supabase REST/RPC calls use the standard library (urllib) -- no client lib.
# The Open-Meteo weather fetch (v0.5, gss/weather_feed.py) also uses urllib --
# still no new dependency.
